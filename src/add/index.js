/* 
	add function
	
	* Create a function that will return the sum of all the arguments.
	* add() should take two arguments
	* It's ok to assume the arguments passed in are numbers.
*/

function add () {
	// Your Code Here
}

module.exports = add;