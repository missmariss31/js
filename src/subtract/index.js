/* 
	subtract function
	
	* Create a function that will return the difference of all the arguments.
	* subtract() should take two arguments
	* It's ok to assume the arguments passed in are numbers.
*/

function subtract () {
	// Your Code Here
}

module.exports = subtract;